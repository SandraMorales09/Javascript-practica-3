function generarTablaMultiplicar(numero) {
    for (let i = 1; i <= 10; i++) {
        console.log(`${numero} x ${i} = ${numero * i}`);
    }
}

// Ejemplo de uso
generarTablaMultiplicar(5);
// Resultado: Genera la tabla del 5
